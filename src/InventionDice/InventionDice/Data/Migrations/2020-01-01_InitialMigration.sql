CREATE TABLE Dice
(
	 [Id] INTEGER PRIMARY KEY
	,[Name] NVARCHAR(MAX) NOT NULL
);

CREATE TABLE DiceValues
(
	 [Id] INTEGER PRIMARY KEY
	,[DiceId] INTEGER NOT NULL
	,[Value] NVARCHAR(MAX)
	,CONSTRAINT [FK_Dice_DiceValue] FOREIGN KEY([DiceId]) REFERENCES [Dice]([Id])
);